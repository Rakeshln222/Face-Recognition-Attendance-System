import mysql.connector
conn = mysql.connector.connect(host="localhost", user="root", password="pw", database="attendance_db")
cur = conn.cursor()
cur.execute("CREATE TABLE IF NOT EXISTS attendance (id INT AUTO_INCREMENT PRIMARY KEY, name VARCHAR(100), date DATE, time TIME, timestamp DATETIME DEFAULT CURRENT_TIMESTAMP)")
conn.commit()
# and use cur.execute with %s placeholders
